%ul.nav.nav-tabs
  - show_tabs.each do |tab, icon|
    - is_active = tab == active_tab
    %li{class: is_active ? 'active' : ''}
      = link_to(tab: tab) do
        %span{class: "glyphicon glyphicon-#{icon}"}
        = t(".tabs.#{tab}")

.page-header
  %h1= t('.title', tournament: shown_tournament.title)

.container
  = render 'info_header', tournament: decorate(shown_tournament)
  = render active_tab
